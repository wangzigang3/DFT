# Examples

Use this directory for small, safe demonstration inputs or parser fixtures.

Avoid committing complete real calculation folders. If an example needs output-like text, keep it tiny and synthetic so the repository stays lightweight.
